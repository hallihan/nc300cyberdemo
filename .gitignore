node_modules
node-app.tar

# Generated by `npm run build`. CI builds this before packaging, so it is no
# longer committed. package-lock.json is deliberately NOT ignored: `npm ci`
# requires it, and it makes the build reproducible.
build/
